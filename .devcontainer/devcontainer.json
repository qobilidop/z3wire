{
  "name": "z3wire",
  "build": {
    "dockerfile": "Dockerfile"
  },
  "remoteUser": "dev",
  "workspaceMount": "source=${localWorkspaceFolder},target=/workspace,type=bind",
  "workspaceFolder": "/workspace",
  "mounts": [
    "source=bazel-cache,target=/home/dev/.cache/bazel,type=volume"
  ],
  "forwardPorts": [8000]
}
